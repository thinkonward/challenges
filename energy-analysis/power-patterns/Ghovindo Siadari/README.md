# onward_demand_response_competition

## Setup

Add this directory to PYTHONPATH
```
export PYTHONPATH=/path/to/your/directory
```

Install the required libraries
```
sudo apt-get install graphviz 
pip install -r requirements.txt
```
